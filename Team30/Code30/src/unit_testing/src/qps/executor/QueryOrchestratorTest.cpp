#include "catch.hpp"

#include <unordered_map>

#include "qps/executor/QueryOrchestrator.h"

using std::unordered_map;
// TODO(KwanHW): Include multiple queries test

TEST_CASE("Queries with Select only") {
//  PQLQuery* query;
//  PQLQueryVariable targetVariable;

  // Statement
//  targetVariable = {PQL_VAR_TYPE_STMT, "s"};
//  query = new PQLQuery(
//      unordered_map<string, PQLQueryVariable>({{"s", targetVariable}}),
//      targetVariable,
//      vector<shared_ptr<Clause>>());

  // Read
  // Print
  // Call
  // While
  // If
  // Assign

  // Constant
  // Procedure
  // Variable
}

TEST_CASE("Queries that uses target declaration variable") {

}

TEST_CASE("Queries that do not use target declaration variable") {

}
